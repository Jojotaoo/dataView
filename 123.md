# ECharts Map 样式映射文档（从 SVG 迁移到 ECharts）

> 本文档从 `src/views/screen/index.vue` 中提取的地图组件样式，整理为可直接映射到 ECharts Map（`geo` / `map` 系列）的配置项。

---

## 一、地区区域样式（`geo` → `itemStyle` / `emphasis` / `select`）

原代码位置：`getCityFill()` 和 `getCityStroke()`

| 状态 | ECharts 属性 | 填充色（`areaColor`） | 描边色（`borderColor`） | 描边宽度（`borderWidth`） |
|------|-----------|-------------------|---------------------|---------------------|
| **默认** | `itemStyle` | `rgba(0,60,120,0.3)` | `rgba(0,128,255,0.45)` | `1` |
| **悬停** | `emphasis.itemStyle` | `rgba(0,160,255,0.3)` | `#40b0ff` | `1` |
| **选中** | `select.itemStyle` | `rgba(0,180,255,0.35)` | `#00c8ff` | `2` |
| **其他未选中** | `itemStyle`（条件判断） | `rgba(0,50,100,0.2)` | `rgba(0,100,180,0.2)` | `1` |

**悬停额外效果：** `filter: brightness(1.2)` → ECharts 中可映射为 `emphasis.itemStyle.opacity` 或 `shadowBlur`

---

## 二、选中城市呼吸动画（`animation`）

```javascript
// 原代码动画：cityBreath3D 2s ease-in-out infinite
// filter 切换：url(#cityGlowStrong) brightness(1) ↔ url(#cityGlow) brightness(1.15)
```

| 属性 | ECharts 映射 |
|------|-------------|
| 发光半径变化 | `select.itemStyle.shadowBlur: 4 ↔ 6` |
| 亮度变化 | `select.itemStyle.shadowColor` 亮度调整 |
| 动画循环 | `animation: true`, `animationDuration: 2000`, `animationEasing: 'cubicInOut'` |

---

## 三、Tooltip 样式（`tooltip`）

| 原样式 | ECharts `tooltip` 配置 |
|-------|---------------------|
| 背景色 `rgba(0,32,71,0.92)` | `backgroundColor: 'rgba(0,32,71,0.92)'` |
| 边框 `1 solid rgba(0,200,255,0.4)` | `borderColor: 'rgba(0,200,255,0.4)'`, `borderWidth: 1` |
| 圆角 `6px` | `borderRadius: 6` |
| 内边距 `12px 16px` | `padding: [12, 16]` |
| 阴影 `0 4px 20px rgba(0,200,255,0.1)` | `extraCssText: 'box-shadow: 0 4px 20px rgba(0,200,255,0.1); backdrop-filter: blur(8px)'` |
| 最小宽度 `180px` | `minWidth: 180` |
| 标题颜色 `#00c8ff` / 14px / 600 | `textStyle.color`（需要自定义 formatter）|
| 行颜色 `rgba(255,255,255,0.7)` / 12px | 同上 |

**Tooltip 中标题下方的分隔线：** `border-bottom: 1px solid rgba(0,200,255,0.2)` → 需要在 `formatter` 的 HTML 模板中实现

---

## 四、标注点（散点 / `scatter` / `effectScatter`）

| 元素 | 样式 |
|------|------|
| 外圈 | `r: 8`, `fill: 'rgba(0,128,255,0.06)'` (选中时 `rgba(0,200,255,0.12)`) |
| 阴影圆圈 | `r: 3.5` (选中 `4.5`), `fill: 'rgba(0,0,0,0.3)'` |
| **主圆点** | `r: 3` (选中 `4`), `fill: '#0080ff'` (悬停/选中 `#00c8ff`) |
| 高光白点 | `r: 1.2` (选中 `1.8`), `fill: 'rgba(255,255,255,0.5)'` |
| 圆点阴影 | `drop-shadow(0 1px 2px rgba(0,0,0,0.4))` → `shadowBlur: 2`, `shadowColor: 'rgba(0,0,0,0.4)'`, `shadowOffsetY: 1` |
| 选中脉冲 | `animation: dotPulse3D 1.5s ease-in-out infinite` → 使用 `effectScatter` 的 `rippleEffect` |

---

## 五、城市名称文字（`label`）

| 属性 | ECharts 配置 |
|------|-------------|
| 颜色（默认） | `rgba(255,255,255,0.75)` |
| 颜色（悬停/选中） | `#00c8ff` |
| 字号（默认） | `10` |
| 字号（选中） | `12` |
| 字重（默认） | `500` |
| 字重（选中） | `700` |
| 位置 | 城市中心点上方偏移 `-10px` |
| 对齐 | `text-anchor: 'middle'` |

---

## 六、其他未选中区域变暗

原代码：`.city-dimmed { opacity: 0.2; }`

```javascript
// ECharts 映射：
// 通过 select 事件或数据状态控制
itemStyle: {
  opacity: selectedCity ? 0.2 : 1
}
```

---

## 七、颜色汇总（保留值）

| 用途 | 颜色 |
|-----|------|
| 主题蓝（悬停/选中） | `#00c8ff` |
| 默认地图蓝 | `#0080ff` |
| 悬停地图蓝 | `#40b0ff` |
| 面板背景蓝 | `rgba(0,32,71,0.6)` |
| Tooltip 背景 | `rgba(0,32,71,0.92)` |
| 危险风险 | `#b71c1c` |
| 高风险 | `#ff5252` |
| 中风险 | `#ffc107` |
| 低风险 | `#00e676` |

---

## 八、待确认事项

1. **3D 侧面厚度 & 3D 高光渐变**：ECharts Map 无法直接实现 SVG 滤镜/3D投影效果，需要确认如何处理。
2. **涟漪动画**：ECharts `effectScatter` 的 `rippleEffect` 可以模拟，需要确认颜色是否保持 `#00c8ff`。
3. **选中城市后的缩略省份地图**：是否需要单独画一个 ECharts 小地图？
4. **呼吸动画的亮度变化**：ECharts 可以通过 `animation` 实现颜色和 `shadowBlur` 的周期性变化，需要确认是否接受这种替代方案。

---

## 九、ECharts 配置示例（参考）

```javascript
option = {
  tooltip: {
    trigger: 'item',
    backgroundColor: 'rgba(0,32,71,0.92)',
    borderColor: 'rgba(0,200,255,0.4)',
    borderWidth: 1,
    borderRadius: 6,
    padding: [12, 16],
    textStyle: {
      color: '#ffffff',
      fontSize: 12
    },
    extraCssText: 'box-shadow: 0 4px 20px rgba(0,200,255,0.1); backdrop-filter: blur(8px);'
  },
  geo: {
    map: 'heilongjiang',
    roam: true,
    label: {
      show: true,
      color: 'rgba(255,255,255,0.75)',
      fontSize: 10,
      fontWeight: 500,
      formatter: function(params) {
        return params.name;
      }
    },
    emphasis: {
      label: {
        show: true,
        color: '#00c8ff',
        fontSize: 12,
        fontWeight: 700
      },
      itemStyle: {
        areaColor: 'rgba(0,160,255,0.3)',
        borderColor: '#40b0ff',
        borderWidth: 1,
        shadowBlur: 6,
        shadowColor: 'rgba(0,200,255,0.3)'
      }
    },
    select: {
      label: {
        show: true,
        color: '#00c8ff',
        fontSize: 12,
        fontWeight: 700
      },
      itemStyle: {
        areaColor: 'rgba(0,180,255,0.35)',
        borderColor: '#00c8ff',
        borderWidth: 2,
        shadowBlur: 8,
        shadowColor: 'rgba(0,200,255,0.4)'
      }
    },
    itemStyle: {
      areaColor: 'rgba(0,60,120,0.3)',
      borderColor: 'rgba(0,128,255,0.45)',
      borderWidth: 1
    }
  },
  series: [{
    type: 'effectScatter',
    coordinateSystem: 'geo',
    data: [{
      name: '哈尔滨市',
      value: [126.6425, 45.7572]
    }],
    symbolSize: 6,
    showEffectOn: 'render',
    rippleEffect: {
      brushType: 'stroke',
      scale: 3,
      period: 1.5
    },
    itemStyle: {
      color: '#00c8ff',
      shadowBlur: 2,
      shadowColor: 'rgba(0,0,0,0.4)',
      shadowOffsetY: 1
    },
    label: {
      show: true,
      position: 'top',
      distance: 10,
      formatter: '{b}',
      color: 'rgba(255,255,255,0.75)',
      fontSize: 10,
      fontWeight: 500
    }
  }]
};
```
